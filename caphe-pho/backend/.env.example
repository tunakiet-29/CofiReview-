# ── Bắt buộc thay đổi khi deploy ──────────────────────────
JWT_SECRET=thay_bang_chuoi_ngau_nhien_dai_it_nhat_32_ky_tu

# ── Server ─────────────────────────────────────────────────
PORT=3001
NODE_ENV=development
FRONTEND_URL=http://localhost:5173

# ── Database (bỏ trống = dùng SQLite local) ───────────────
# DATABASE_URL=postgresql://user:password@localhost:5432/caphe_pho

# ── Redis (bỏ trống = dùng in-memory cache) ──────────────
# REDIS_URL=redis://localhost:6379
